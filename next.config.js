/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "mikemulligan.net"],
  },
  pageExtensions: ["js", "jsx", "mdx", "ts", "tsx"],
};

module.exports = nextConfig;
